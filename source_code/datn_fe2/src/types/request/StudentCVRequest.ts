export type StudentCVRequest = {
    firstName?: string,
    surName?: string,
    lastName?: string,
    phoneNumber?: string,
    address?: string,
    prize?: string,
    certificate?: string,
    // học vấn
    study?: string,
    talent?: string,
    weakness?: string,
    hobby?: string,
    additional?: string,
}